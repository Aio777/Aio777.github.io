---
interface Props { diagonal?: boolean; class?: string }
const { diagonal = false, class: className = '' } = Astro.props;
---
<svg class:list={['arrow-icon', className]} viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d={diagonal ? 'M5 19 19 5M5 5h14v14' : 'M4 12h16M13 5l7 7-7 7'} stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>
